{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "postinstall": "apt-get update && apt-get install -y python3-pip && pip3 install yt-dlp"
    "start": "nodemon api/index.js",
    "dev": "nodemon api/index.js",  
    "postinstall": "pip install yt-dlp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
